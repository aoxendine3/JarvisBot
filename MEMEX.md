# Project Memex: env-integrity-sentry

**This document is the primary memory buffer for the project. Read this before every execution.**

## 1. Governance & Constraints
- **Locked Assets**: `bin.cjs`, `README.md`, `package.json`, `action.yml`, `server.cjs`.
- **Authorization**: **PIN 3873** required for any modification to locked assets.
- **Language Mandate**: **Boringly Reliable**. No narrative fluff, no "institutional/forensic/sovereign" adjectives unless measurable.
- **Role**: Technical execution engine. No "amazement" or persona resets.

## 2. Technical Architecture (v1.1.5)
- **Engine**: Node.js CLI with context-aware logic (Expected vs. Unexpected).
- **Integrity**: `--verify` flag checks hashes against `integrity.lock`.
- **Data Layer**: `--upload <URL>` transmits JSON reports to `server.cjs`.
- **Storage**: `data/` directory contains timestamped repository snapshots.

## 3. Knowledge Base (Already Known)
- **Hugging Face Trends**: Integration of `granite-embedding` and `Llama-3.1` for semantic secret detection is the defined ROI path.
- **Market Proof**: `shadcn/taxonomy` audit established "Setup Discovery" value.
- **Internal Audit**: `XORAS_SOV_CORE` has 110+ missing dependencies. `AntiGravity` requires `.env.example` guardrails.

## 4. Continuity Instruction
- Do not react as if information is new.
- Cross-reference current state against `integrity.lock` and `data/` snapshots.
- Prioritize "Does this save me from a mistake?" over "Is this powerful?".
